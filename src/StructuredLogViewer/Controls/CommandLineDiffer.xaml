<UserControl x:Class="StructuredLogViewer.Controls.CommandLineDiffer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StructuredLogViewer.Controls"
             xmlns:a="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <DockPanel Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="topMenuBar">
            <CheckBox x:Name="caseSensitiveCheckBox" 
               Margin="4"
               VerticalAlignment="Center"
               DockPanel.Dock="Left"
               Content="Case Sensitive" />
        </DockPanel>

        <a:TextEditor x:Name="textEditorLeft"
                 FontFamily="Consolas"
                 Grid.Row="1"
                 ShowLineNumbers="True"
                 LineNumbersForeground="Teal"
                 FontSize="14">
        </a:TextEditor>

        <a:TextEditor x:Name="textEditorRight"
                 FontFamily="Consolas"
                 Grid.Row="2"
                 ShowLineNumbers="True"
                 LineNumbersForeground="Teal"
                 FontSize="14">
        </a:TextEditor>

        <DockPanel Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="middleMenuBar">
            <Button x:Name="CompareNow"
               Content="Compare"
               Padding="6,3,6,3"
               Click="CompareNow_Click"
               BorderThickness="0"
               Background="Transparent"
               VerticalAlignment="Center"
               DockPanel.Dock="Left"
               ToolTip="Compare command lines."/>
            <!--<CheckBox x:Name="wordWrap" 
               Margin="4"
               VerticalAlignment="Center"
               DockPanel.Dock="Left"
               Content="Word Wrap"
               Checked="wordWrap_Checked"
               Unchecked="wordWrap_Unchecked"/>-->
        </DockPanel>
        
        <a:TextEditor x:Name="textEditorResult"
                 FontFamily="Consolas"
                 Grid.Row="3"
                 ShowLineNumbers="True"
                 LineNumbersForeground="Teal"
                 FontSize="14"
                 IsReadOnly="true">
        </a:TextEditor>
    </Grid>
</UserControl>
